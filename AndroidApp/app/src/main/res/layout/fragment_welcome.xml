<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_blue_soft"
    android:padding="24dp">

    <!-- ===== Guideline 30% chiều cao ===== -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guide30"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.22" />

    <!-- ===== Title SMART ACCESS + Icon ===== -->
    <LinearLayout
        android:id="@+id/titleContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="SMART ACCESS"
            android:textSize="35sp"
            android:textStyle="bold"
            android:textColor="#2B2B2B"/>

        <ImageView
            android:id="@+id/imgShield"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_marginStart="10dp"
            android:src="@drawable/ic_unlock"
            android:contentDescription="@null"/>
    </LinearLayout>

    <!-- ===== OWPA Button ===== -->
    <!-- ===== OWPA Button (Custom) ===== -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/btnOwpa"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:focusable="true"
        app:cardCornerRadius="32dp"
        app:cardElevation="6dp"
        app:cardBackgroundColor="#651FFF"
        app:layout_constraintTop_toBottomOf="@id/guide30"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingVertical="20dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="OWPA"
                android:textSize="28sp"
                android:textColor="#FFFFFF"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="One-Way Pairing Access"
                android:textSize="20sp"
                android:textColor="#EDE7FF"
                android:layout_marginTop="4dp"/>
        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>


    <!-- ===== Car Image Circle ===== -->
    <FrameLayout
        android:id="@+id/carContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/bg_circle_border"
        android:padding="20dp"
        app:layout_constraintTop_toBottomOf="@id/btnOwpa"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintDimensionRatio="1:1"
        android:layout_marginTop="24dp">

        <ImageView
            android:id="@+id/imgCar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/car_key_central"
            android:scaleType="fitCenter"
            android:scaleX="1.3"
            android:scaleY="1.3"
            android:contentDescription="@null"/>
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
